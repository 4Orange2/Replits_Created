# this program aims to find the length of a number

from math import *

number = input("What is the number that you want to find the length of? ")

length = floor(log(200,10))

print(f"this is your length {length}")